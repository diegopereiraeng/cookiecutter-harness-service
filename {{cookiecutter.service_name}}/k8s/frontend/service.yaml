apiVersion: v1
kind: Service
metadata:
  name: {{ cookiecutter.service_name }}-web
  namespace: {{ cookiecutter.harness_project }}
  labels:
    app: {{ cookiecutter.service_name }}-web
    app.kubernetes.io/name: {{ cookiecutter.service_name }}-web
spec:
  selector:
    app: {{ cookiecutter.service_name }}-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
